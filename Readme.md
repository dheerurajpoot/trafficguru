# 1. Install all dependencies

npm install

# 2. Install Playwright browser binaries

npx playwright install chromium

# 3. Dev mode (opens Electron with Vite hot reload)

npm start

# 4. Build installer

npm run build

# → dist/TrafficBotSetup.exe (Windows)

# → dist/TrafficBot.dmg (macOS)
# trafficguru
